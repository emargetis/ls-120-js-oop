/*
`call` which takes arguments of the context object and other comma-separated 
arguments to be supplied to the method / function and `apply` which behaves 
similarly to `call` except the additional arguments after the context object
are supplied as an array
*/